import * as addresses from "../../address-service";
export const addressService = { list: (userId: number) => addresses.listCustomerAddresses(userId), create: (userId: number, input: Parameters<typeof addresses.createCustomerAddress>[1]) => addresses.createCustomerAddress(userId, input), update: (userId: number, input: Parameters<typeof addresses.updateCustomerAddress>[1]) => addresses.updateCustomerAddress(userId, input), setDefault: (userId: number, addressId: number) => addresses.setDefaultCustomerAddress(userId, addressId), archive: (userId: number, addressId: number) => addresses.archiveCustomerAddress(userId, addressId) };
